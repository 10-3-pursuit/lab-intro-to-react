import React from 'react';

const ContactUserCard = ({ user }) => {
  return (
    <div className="contact-user-card">
      <h3>{user.name}</h3>
      
    </div>
  );
};

export default ContactUserCard;
